{
  "name": "ollagraph-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node scripts/gen-sitemap.mjs && node scripts/inject-stats.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=ollagraph-marketing --branch=main && node scripts/purge-cache.mjs",
    "prebuild": "node scripts/check-bundles.mjs && node scripts/check-inline-styles.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "sharp": "^0.35.2"
  }
}
