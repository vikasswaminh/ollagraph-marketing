{
  "name": "ollagraph-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node scripts/gen-sitemap.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=ollagraph-marketing --branch=main && node scripts/purge-cache.mjs",
    "prebuild": "node scripts/check-bundles.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^4.16.18"
  }
}
