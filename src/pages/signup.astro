---
import BaseLayout from '../layouts/BaseLayout.astro';
import bodyHtml from '../bodies/signup.body.html?raw';
import headExtras from '../bodies/signup.head.html?raw';

const rendered = bodyHtml;
---
<BaseLayout title="Create your account — Ollagraph">
  <Fragment slot="head" set:html={headExtras} />
  <Fragment set:html={rendered} />
</BaseLayout>
