---
import BaseLayout from '../layouts/BaseLayout.astro';
import bodyHtml from '../bodies/login.body.html?raw';
import headExtras from '../bodies/login.head.html?raw';

const rendered = bodyHtml;
---
<BaseLayout title="Log in — Ollagraph">
  <Fragment slot="head" set:html={headExtras} />
  <Fragment set:html={rendered} />
</BaseLayout>
